using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum BoosterType { Food = 0, Petting, AnimalVaccine, Boost1 }
public enum BoosterState { CoolDown, Using, Ready }
