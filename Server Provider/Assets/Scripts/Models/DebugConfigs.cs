
public static class DebugConfigs
{
    // Set true this variable if you want to reset UI before running game.
    public static bool RESET_UI = false;

    // Set true this variable if you want to show log messages.
    public static bool DEBUG_LOG = false;
}